<a class="navbar-menu-item left" href="{{ path('message_conversations') }}">
    <i class="icon-chevron-left" style="font-size: 22px"></i>
</a>

<div class="navbar-menu-title">
    <h1>
        <a href="{{ path('user_profile', {'username':user.username}) }}">{{ user.username }}</a>

        {% if user.profile.verified == true %}
            <span class="verified">
                <i class="icon-check-circle-two text-info"></i>
            </span>
        {% endif %}
    </h1>
</div>
