{% if title == 'account.deleting' or title == 'password.resetting' %}
    <a class="navbar-menu-item left" href="{{ path('user_settings', {'username':app.user.username}) }}">
        <i class="icon-chevron-left" style="font-size: 22px"></i>
    </a>
{% elseif title == 'bookmarks' %}
    <a class="navbar-menu-item left" href="{{ path('user_profile', {'username':app.user.username}) }}">
        <i class="icon-chevron-left" style="font-size: 22px"></i>
    </a>
{% else %}
    <a class="navbar-menu-item left" href="{{ path('user_profile', {'username':user.username}) }}">
        <i class="icon-chevron-left" style="font-size: 22px"></i>
    </a>
{% endif %}

<div class="navbar-menu-title">
    <h1>{{ title | trans }}</h1>
</div>
