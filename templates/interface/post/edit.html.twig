{% extends 'interface/base.html.twig' %}

{% block title %}{{ 'post.editing' | trans }}{% endblock %}

{% block body %}

    <h1>{{ 'post.editing' | trans }}</h1>

    {{ include('interface/post/_form.html.twig') }}

{% endblock %}

{% block scripts %}
    {{ ckeditor('user') }}
    {{ chosen() }}
    {{ chosen(10, '.chosen-music') }}
    {{ chosen(3,'.chosen-date select') }}
{% endblock %}
