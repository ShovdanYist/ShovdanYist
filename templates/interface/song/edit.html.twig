{% extends 'interface/base.html.twig' %}

{% block title %}{{ song.fullTitle }} | {{ 'song.editing' | trans }}{% endblock %}

{% block body %}

    <h1>{{ 'song.editing' | trans }}</h1>

    {{ include('interface/song/_form_song.html.twig') }}

{% endblock %}

{% block scripts %}
    {{ ckeditor() }}
    {{ chosen(10) }}
{% endblock %}
